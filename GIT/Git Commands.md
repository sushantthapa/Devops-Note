```
fork the repository into your bit bucket
clone the repository
set the maven version in the intelliji
set the upstream ------ git remote add upstream <URL>
git fetch upstream
git checkout -b dev 
git push origin dev

```